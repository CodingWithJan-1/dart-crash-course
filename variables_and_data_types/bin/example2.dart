void main(List<String> args) {
  final age = 300;
  print(age);
  // final variables can only be set once and dont allow re-assigning of value
  // age = 13;


  final numList = [1,2,3];
  print(numList);

  numList.removeAt(0);
  print(numList);

}